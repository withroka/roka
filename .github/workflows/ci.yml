name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions: {}

jobs:
  test:
    strategy:
      matrix:
        component: [core, tool]

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup
        uses: denoland/setup-deno@v2

      - name: Test
        run: deno task ok ${{ matrix.component }}

      - name: Coverage
        uses: codecov/codecov-action@v6
        with:
          fail_ci_if_error: true

  build:
    strategy:
      matrix:
        component: [tool]

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup
        uses: denoland/setup-deno@v2

      - name: Build
        run: deno task -q forge compile ${{ matrix.component }}/*
