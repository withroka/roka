/**
 * @deprecated Moved to `@roka/fs/temp`. This file will be removed in a future
 * release. Update your imports:
 * ```diff
 * - import { tempDirectory } from "@roka/testing/temp";
 * + import { tempDirectory } from "@roka/fs/temp";
 * ```
 */
export { type TempDirectory, tempDirectory } from "@roka/fs/temp";
