/**
 * This is a placeholder file to make sure that `deno test` works in this directory.
 *
 * ```ts
 * import { test } from "./test.ts";
 * 
 * test({
 *   name: "my test",
 *   fn() {
 *     // test code here
 *   },
 * });
 * ```
 */

export function test() {}
