/**
 * Comment example that will fail all checks
 *
 * ```ts
 * if (42 !== 43) throw new Error("fail");
 *
 * function example( ): number {
 *    const a: string = window?.location;
 *    return "42";
 * }
 * ```
 */
